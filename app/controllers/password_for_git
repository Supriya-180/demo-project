basic commands for git -->

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Supriya-180/myproject.git
git push -u origin main
git status
git branch
git checkout -b devolop (git -b branch_name and git checkout branch_name)
git status
git add .
git commit -m "new repo"
git push origin devolop 
history

# git stash push "file_name"   # for switch to another branch and changes done on current branch
# git stash pop --> f


git commit --amend -m "New commit message."    (for change commit message before push)

git push --force-with-lease origin EXAMPLE-BRANCH   (for change commit message after push)





password for git --> (nov 9)

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